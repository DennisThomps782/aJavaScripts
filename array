nclude:

pop(): Removes and returns the last element from the array.
javascript
Copy code
const lastElement = rows1.pop();
shift(): Removes and returns the first element from the array.
javascript
Copy code
const firstElement = rows1.shift();
unshift(): Adds one or more elements to the beginning of the array.
javascript
Copy code
rows1.unshift(99);
splice(): Adds or removes elements from the array at a specified index.
javascript
Copy code
rows1.splice(2, 0, 555); // Adds 555 at index 2
concat(): Concatenates two or more arrays.
javascript
Copy code
const newArray = rows1.concat([10, 20, 30]);
slice(): Returns a shallow copy of a portion of an array into a new array.
javascript
Copy code
const slicedArray = rows1.slice(1, 3); // Returns elements from index 1 to 2 (exclusive)
forEach(): Executes a provided function once for each array element.
javascript
Copy code
rows1.forEach(item => console.log(item));
map(): Creates a new array with the results of calling a provided function on every element in the calling array.
javascript
Copy code
const doubledArray = rows1.map(item => item * 2);
